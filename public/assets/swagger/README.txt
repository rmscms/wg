Swagger UI (local bundle)
=========================

Files served from the panel — no external CDN at runtime.

- swagger-ui.css
- swagger-ui-bundle.js

Version: 5.18.2 (swagger-ui-dist)

To (re)download:

    bash scripts/download-swagger-ui.sh

Install script runs this automatically if files are missing.
